{
  "schema_version": "wig_p3_bounded_single_project_pilot_write_path_inventory.v3",
  "gate_id": "WIG-P3-AUTHORITATIVE-SINGLE-PROJECT-PILOT-S1-R3",
  "default_policy": "deny",
  "paths": [
    {"id": "http_jsonrpc_tools_call", "policy": "token plus exact 14 tools plus Scope/Lease request capability", "lease_transaction_required": true},
    {"id": "direct_named_jsonrpc_method", "policy": "disabled", "lease_transaction_required": false},
    {"id": "http_actions", "policy": "disabled", "lease_transaction_required": false},
    {"id": "mcp_resources", "policy": "empty or disabled", "lease_transaction_required": false},
    {"id": "agent_tool_dispatch", "policy": "same exact 14 tools and capability", "lease_transaction_required": true},
    {"id": "routed_project_dispatch", "policy": "one exact registry project no root override same allowlist", "lease_transaction_required": true},
    {"id": "compatibility_alias", "policy": "disabled", "lease_transaction_required": false},
    {"id": "work_item_command_gateway", "policy": "Scope mode plus request capability", "lease_transaction_required": true},
    {"id": "work_item_application_service", "policy": "Scope semantic rules and Lease checked inside BEGIN IMMEDIATE", "lease_transaction_required": true},
    {"id": "direct_application_service_construction", "policy": "deny without exact Pilot Lease and request capability", "lease_transaction_required": true},
    {"id": "direct_repository_domain_write", "policy": "not exposed and architecture denied", "lease_transaction_required": false},
    {"id": "first_work_item_create", "policy": "domain facts and nullable-to-generated Lease ID binding atomic", "lease_transaction_required": true},
    {"id": "task_version_external_associations", "policy": "Task digest enforced and external associations denied", "lease_transaction_required": true},
    {"id": "execution_attempt_creation", "policy": "exact external execution receipt Schema, issuer, revocation, slot and scope revalidated; Attempt ID slot binding atomic", "lease_transaction_required": true},
    {"id": "dispatch_authority_read", "policy": "active Lease plus current receipt/project/task recheck; no mutation", "lease_transaction_required": false},
    {"id": "nested_completion_artifact", "policy": "same transaction global Artifact quota and path/digest policy", "lease_transaction_required": true},
    {"id": "gate_derived_audit_outbox_manifest", "policy": "same CAS transaction and reconciled counts no worker dispatch", "lease_transaction_required": true},
    {"id": "backup", "policy": "control plane only generation-bound SQLite Backup API", "lease_transaction_required": false},
    {"id": "restore", "policy": "denied from prepared through Closeout", "lease_transaction_required": false},
    {"id": "migration", "policy": "two explicit forward-only v5-to-v6 and v6-to-v7 migrations matching both exact storage contracts; preserve legacy tables; complete before authorization issuance and Pilot Lease", "lease_transaction_required": false},
    {"id": "preview_signing_key_rotation", "policy": "preprovision only; denied during activation", "lease_transaction_required": false},
    {"id": "external_executor", "policy": "separate exact authorization and path boundary; not composed by Pilot endpoint", "lease_transaction_required": false},
    {"id": "delivery_outbox_worker", "policy": "not composed or started", "lease_transaction_required": false},
    {"id": "product_submission", "policy": "tool and worker absent", "lease_transaction_required": false},
    {"id": "stable_promotion", "policy": "tool and worker absent after acceptance", "lease_transaction_required": false},
    {"id": "git_file_plan_project_mutation", "policy": "Pilot tools absent; external execution separately bounded", "lease_transaction_required": false},
    {"id": "connector_tunnel_relay_proxy", "policy": "not composed loopback only", "lease_transaction_required": false},
    {"id": "decision_consumption_and_tombstone", "policy": "one-shot control plane before Lease; permanent no retry", "lease_transaction_required": false},
    {"id": "fact_reconciliation", "policy": "exact direct counts, derived equations and denied tables before and after every write", "lease_transaction_required": true},
    {"id": "semantic_validator", "policy": "pre-import plus every mutable boundary; rule mismatch or incomplete PASS receipt denies", "lease_transaction_required": true},
    {"id": "project_local_ledger_git_state", "policy": "ignored untracked unstaged before and after", "lease_transaction_required": false},
    {"id": "token_revocation_and_shadow_closeout", "policy": "terminal control plane; listener stopped and evidence preserved", "lease_transaction_required": false}
  ],
  "coverage_requirements": {
    "source_work_item_write_commands": 16,
    "matrix_entries": 16,
    "unclassified_source_commands": 0,
    "unknown_future_tool_or_write_path": "fail_architecture_and_startup",
    "all_allowed_domain_writes_reach_same_transactional_lease": true,
    "no_background_mutator": true
  }
}
