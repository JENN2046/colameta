版本：v1.0 v1.0 COMMANDER_AUTHORIZATION｜E2C_PLAN

用户需求：
COMMANDER_AUTHORIZATION｜E2C_PLAN_PATCH_AND_STATE_ACTION_GUARD_CORE

Goal: Implement Web dangerous-action preview-confirm-audit protection for ColaMeta plan, patch, and state-transition Web actions.

Target routes:
- /api/auto-apply-patches
- /api/reload-plan
- /api/continue-next-version
- /api/rerun-acceptance
- /api/checkpoint-review

Also cover /api/jobs/start aliases for rerun_acceptance and checkpoint_review, or block those aliases until guarded.

Required behavior:
- missing confirmation blocks all five direct routes before service dispatch or mutation
- /api/jobs/start rerun_acceptance/checkpoint_review aliases are guarded before job/state/thread creation
- stale state/plan/patch signatures are rejected
- mutation is avoided before valid confirmation
- E2A/E2B tests still pass
- no executor, commit, push, preview/apply leakage
- redacted dangerous_action_receipt only; no confirmation id leakage

Preserve caveats:
- E1D GitHub Actions caveat remains
- E2A confirmation id browser-visible caveat remains
- E2B protects pre-executor Web confirmation only and does not replace post-run scope validation
- server-side E2A/E2B workdir sync is deferred by Commander decision

Implementation must create one local commit only if validation passes. Do not push.

允许修改文件：
- runner/web_console.py
- runner/dangerous_action_guard.py
- runner/web_console_v2_assets.py
- tests/test_dangerous_action_guard.py
- tests/test_web_console_security.py
- README.md
- README.zh-CN.md

禁止修改范围：
- 未列入 allowed_files 的文件
- runtime/log/token/OAuth/session/state 文件

Out of scope：
- MCP parity
- Git local/remote guard
- executor internals
- provider adapter sandboxing
- workflow file changes
- release/tag/upstream PR
- server-side workdir synchronization

验收命令：
- python3 -m compileall -q runner scripts adapters schemas tests
- python3 -m unittest discover -s tests
- python3 scripts/self_hosting_smoke.py
- git diff --check
- git status --short

执行要求：
- 所有写操作先 preview 再 apply/run
- 输出修改文件列表、实现摘要、测试结果、风险和后续工作
- 禁止自行提交 git commit
- 禁止破坏性 git 命令（reset/clean/push/merge/rebase/checkout/switch/stash）
- 禁止创建、打印或保存 secrets
