Implement v1.15: Stage 9 Next Action / Continue Readiness Report V1.

Goal:
Add a Stage 9 read-only readiness report that tells Web GPT, Codex, planner, reviewer, and Jenn whether the project can continue, why it cannot continue, and what evidence or gate is missing. This version must not continue the runner.

Required behavior:
- Add a read-only continue readiness module.
- Summarize current runner state, current version, next version presence, plan version count, git head/dirty facts when supplied, review decision refs, continue gate refs, taskbook hash refs, and forbidden side effects.
- Return can_continue=false unless there is an explicit eligible review decision or CommanderDecisionRequest, a separate continue gate, matching taskbook hashes or preview refs, no blocking review comments, and a next enabled version.
- For the current completed/no-pending state, return a noop/blocked readiness result explaining that there is no next version to continue into.
- Treat runtime PASSED/COMPLETED as runtime facts only; they must not equal ReviewDecision.ACCEPT or Delivery State accepted.
- If PLAN_ADJUST is present or unresolved, block continue and point to Stage 8 preview.
- Do not call ContinueNextVersionService, StateMachine.continue_next_version, manage_executor_workflow, executor preflight/run, commit, push, deploy, or state mutation.
- Do not add MCP/Web integration in this version.

Scope:
Allowed files are exactly the plan allowed_files. Do not touch forbidden files. This is read-only / preview-only. Do not write plan/state, do not continue executor, do not commit or push.

Validation:
Run the acceptance commands exactly. Report changed files, validation results, and residual risks. Do not commit.
