Task: RUNTIME_VERSION_OBSERVABILITY_PLAN_PREVIEW implementation slice.

Implement a narrow read-only runtime/version observability surface for ColaMeta.

Required behavior:
1. Add a pure read-only runtime observability module that can report:
   - loaded process start time in ISO-8601 form;
   - loaded source HEAD or build HEAD captured for the running process if available;
   - current project checkout HEAD;
   - branch/project root when available;
   - whether restart/reload is needed, preferably true when loaded runtime HEAD differs from current checkout HEAD, false when they match, and unknown when one side is unavailable.
2. Expose the metadata through an existing read-only MCP/status style surface or a narrowly named new read-only runtime/version tool.
3. Do not add Web business routes.
4. Do not add any restart/reload/kill/apply surface.
5. Do not perform Git remote mutation or any fetch/pull/push/tag/release operation.
6. Do not use shell fallback, subprocess shell execution, or direct Git commands for runtime metadata; prefer standard-library file reads and existing safe project identity helpers.
7. Do not expose secrets, tokens, environment values, auth headers, or raw command output.
8. Add focused tests covering matching HEAD, mismatched HEAD, missing/unknown HEAD, process start time presence, no restart side effects, and read-only result shape.

Implementation constraints:
- The runtime observability module must be read-only and side-effect free except for capturing module/process start metadata in memory.
- The surface must be status/metadata only; it must not change running services.
- If modifying MCP server dispatch, add only a read-only tool/surface and keep it explicitly non-mutating.
- Do not touch Web Console, Git remote manager, executor workflow, state mutation, commit manager, or service lifecycle mutation files.

Expected final report:
- list changed files;
- summarize exact fields returned by the runtime observability surface;
- state whether restart_needed is boolean or unknown-capable;
- confirm no restart/reload/kill process behavior was added;
- confirm no business route or remote Git mutation was added;
- include validation command results.
