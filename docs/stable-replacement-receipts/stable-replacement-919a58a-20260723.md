# Stable Replacement Receipt — 919a58a

```yaml
receipt_type: stable_replacement
recorded_on: 2026-07-23
result: passed
authorization:
  scope: "existing App Connector → development MCP real acceptance → stable replacement only after pass"
  target_commit: 919a58a96787e8943923847257cc84692cad28ae
  no_push_tag_release_or_tunnel_restart: true
candidate_traceability:
  dev_repository: /home/jenn/src/colameta-dev
  candidate_head: 919a58a96787e8943923847257cc84692cad28ae
  origin_main_head_observed: e167fa645a000779297918d1b895eabe0756aa55
  candidate_contained_by_local_origin_main: false
  remote_ci_for_exact_candidate: not_verified
  disclosure: "The candidate was locally committed and locally validated; no push was performed."
previous_stable:
  commit: 45e8e42bbcfdee4fa47ff9890cfd84a412ff00bf
  backup_archive: /home/jenn/tools/colameta-stable-backups/colameta-stable-45e8e42-20260723T142852Z.tar.gz
  backup_sha256: 0dab2469762feeb33b802bf5afc1a48c5abb27baf99679d685274c3e02317566
  rollback_ref: colameta-stable-rollback-45e8e42-20260723T142852Z
  preserved_untracked_files:
    - AGENTS.md:Zone.Identifier
    - AGENTS - 副本.md:Zone.Identifier
stable_action:
  local_origin_fetch_exact_commit: passed
  stable_checkout_detached_head: 919a58a96787e8943923847257cc84692cad28ae
  wheel_sha256: e87b98fbd18f11065110b5db668eefebea97945dbef980bc994d952ea7c08e99
  pip_install: "--no-deps --force-reinstall"
  pip_check: passed
  restarted_services:
    - colameta-stable.service
    - colameta-mcp-remote.service
    - colameta-mcp-advanced.service
  tunnel_restarted: false
```

## Candidate validation

- Full test suite: `1969 passed`, `142 subtests passed`; three pre-existing
  pytest temporary-directory cleanup warnings remained non-fatal.
- `ruff check runner tests`: passed.
- `python -m compileall adapters runner schemas scripts tests`: passed.
- `scripts/self_hosting_smoke.py`: passed.
- `git diff --check`: passed.

The strict closeout tests require a clean candidate. Seven pre-existing,
untracked stable-replacement receipts were temporarily isolated for that gate,
then restored byte-for-byte using their SHA-256 manifest. Generated bytecode
caches were isolated outside the checkout before the clean-candidate run; no
tracked source file was changed.

## Real ChatGPT App acceptance before promotion

The existing authenticated App Connector served the candidate HEAD and passed
the read-only acceptance flow:

1. `review_manifest/inspect` returned the bound template for
   `colameta-self-dev` at `919a58a…`.
2. A two-subject external manifest was accepted; both subject pages were read
   through the seven-tool compatibility route and `verify` rechecked the
   complete context and both subject SHA-256 values.
3. A one-character HEAD change was rejected by the inner workflow as
   `CONTEXT_BINDING_MISMATCH`.
4. An 83,992-character `manage_git/diff_commits` result was packaged as a
   seven-page artifact. All pages were recovered through `result_artifact/read`;
   the reconstructed SHA-256 matched the server contract.
5. Sanitized loopback tunnel `/healthz` and `/readyz` evidence produced
   `apps_connector_closeout=ready` and overall connector health `healthy`.

No executor, validation run, Git write, Delivery state, or connector/auth
configuration change was performed during this acceptance.

## Post-replacement verification

- Stable checkout HEAD is exactly `919a58a96787e8943923847257cc84692cad28ae`.
- All three restarted units are `active/running`; their command identities use
  the stable virtual environment. The dev checkout remains only the managed
  project root, not the service package source.
- Loopback Web, focused MCP, external-OAuth MCP, advanced MCP, tunnel
  `/healthz`, and tunnel `/readyz` all passed.
- The real App Connector reported exactly seven Commander tools, returned the
  target HEAD through `review_manifest/inspect`, accepted
  `gate_review_request/inspect` with `read_only=true` and `side_effects=false`,
  and returned connector closeout `ready`.
- The typed artifact compatibility workflow remains present and rejects a
  missing handle with `RESULT_ARTIFACT_ID_REQUIRED`.

## Rollback boundary

Rollback is not automatic authority. If it is separately authorized, restore
the recorded archive or local rollback ref to the exact previous commit,
reinstall the resulting wheel, restart only the recorded stable code services,
and repeat the post-replacement checks.
